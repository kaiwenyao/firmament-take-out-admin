# URL of the customer-facing user frontend, opened by the "User App" link
# in the admin sidebar. Change this when the user app is deployed on
# another host or port (defaults to http://localhost:5173).
VITE_USER_CLIENT_URL=http://localhost:5173
